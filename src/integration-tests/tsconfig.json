{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["node", "jest"],
    "noEmit": true,
    "composite": false
  },
  "include": ["./**/*.ts"],
  "exclude": [
    "../../node_modules",
    "../../dist",
    "../../bundle",
    "../../coverage",
    "../../docs"
  ]
}
